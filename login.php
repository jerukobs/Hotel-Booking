<?php

	//including the server connection string
		//include 'logon.php';	

// checking whether user click on save 
		if (isset($_POST['submit'])) {

// saving values on the form into the table
		     $studentid= $_POST['studentid'];
		     $pin= $_POST['pin'];
		   

		   /* $sql= " INSERT INTO student (memberid, firstname, lastname, age, city, region, comment) 
		    		VALUES ('$memberid','$firstname','$lastname','$age', '$city', '$region', '$comment')";

		    $results= mysqli_query($conn,$sql);
		    if ($results) {
		    	echo "Record has been successfully added";
		    }
		  	
		  } */
	?>




















?>